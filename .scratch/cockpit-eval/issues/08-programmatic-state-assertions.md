# 08: 程序化终态断言评估器（预留）

**What to build:** 座舱后端查询接口可用后启动：实现自定义 Evaluator 子类，通过后端接口查询车况/车控终态，把可查状态任务的期望结果改写为程序化断言，逐步替换这些任务的 NL 判分，压缩 judge 噪声。judge 盲区（黑盒 Agent "没调工具但嘴上说调了"）由终态查询直接证伪。

**Blocked by:** 06（多 trial 批跑基线，作为替换前后的对照基准）+ 外部依赖（后端查询接口可用）

**Status:** ready-for-agent

- [ ] 自定义评估器接入后端查询接口，calculate_reward 产出程序化断言结果
- [ ] 可查终态的任务改写为程序化断言，判分与终态一致
- [ ] 替换后重跑并与 06 基线对照，确认结果一致性在预期内
